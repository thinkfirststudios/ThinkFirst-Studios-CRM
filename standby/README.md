# Standby lists

Lead lists that are ready to import but have nowhere to go yet — usually
because the person who will run them has not made their CRM account.

Nothing here is committed. These files hold contact details for real
people, same as `.tmp/` and `backups/`.

Not `.tmp/`: that directory is disposable by convention, and a list waiting
on somebody's start date is the opposite of disposable.
